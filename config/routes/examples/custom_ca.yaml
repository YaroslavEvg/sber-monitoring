routes:
  - name: httpbin-custom-ca
    type: http
    url: https://httpbin.org/status/418
    method: GET
    interval: 600
    timeout: 10
    ca_bundle: certs/internal-root.pem
    enabled: false
    tags:
      - custom-ca
      - disabled-example
    description: "Пример мониторинга с кастомным CA (по умолчанию выключен)."
